<%= form_for(@coloc) do |f| %>
  <% if @coloc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coloc.errors.count, "error") %> prohibited this coloc from being saved:</h2>

      <ul>
      <% @coloc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :adress %><br>
    <%= f.text_field :adress %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
