<h1>Tous les utilisateurs</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_image_tag user.email, :size => 30 %>
      <%= user.nom %> <%= user.prenom %>
      <%= link_to "Ajouter", pages_add_user_path(:id => user.id) %>
    </li>
  <% end %>
</ul>